version: '3.8'

services:
  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    ports:
      - '3001:3001'
    environment:
      NODE_ENV: production
      PORT: 3001
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
